-- Streams for staged ingestion and materialized-flow processing
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;

CREATE OR REPLACE STREAM DOCGEN.STREAM_SIG_EVENTS_RAW ON TABLE DOCGEN.SIGNATURE_EVENTS_RAW(APPEND_ONLY = TRUE);

CREATE OR REPLACE STREAM DOCGEN.STREAM_SIGN_REQUESTS ON TABLE DOCGEN.SIGNATURE_REQUESTS(APPEND_ONLY = FALSE);

