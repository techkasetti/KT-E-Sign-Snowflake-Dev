-- SQL UDF to compute SHA256 of a string (used for canonical hashes)
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE FUNCTION DOCGEN.COMPUTE_SHA256(val STRING)
RETURNS STRING
LANGUAGE SQL
AS $$
  SELECT LOWER(TO_HEX(SHA2(val,256)))
$$;

