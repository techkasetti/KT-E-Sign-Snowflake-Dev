USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.BIOMETRIC_MATCHES (
  MATCH_ID STRING PRIMARY KEY,
  SIGNER_ID STRING,
  MATCH_SCORE FLOAT,
  MATCHED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP(),
  META VARIANT
);

