USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE VIEW DOCGEN.V_BILLING_OVERVIEW AS SELECT ACCOUNT_ID, SUM(AMOUNT) AS TOTAL_BILLED FROM DOCGEN.BILLING_LINE_ITEM GROUP BY ACCOUNT_ID; -- @1

