USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.ENSEMBLE_ROUTES (
  ROUTE_ID STRING PRIMARY KEY,
  NAME STRING,
  RULES VARIANT,
  PRIORITY INT,
  ACTIVE BOOLEAN DEFAULT TRUE
);

