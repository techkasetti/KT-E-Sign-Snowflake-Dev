USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.SIGNERS (
  SIGNER_ID STRING PRIMARY KEY,
  REQUEST_ID STRING,
  ACCOUNT_ID STRING,
  SIGNER_EMAIL STRING,
  SIGNER_NAME STRING,
  SIGNER_ORDER NUMBER,
  AUTH_METHOD STRING,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);
-- Signer roster table for requests and signer ordering, follows domain model patterns @3 @4 @31

