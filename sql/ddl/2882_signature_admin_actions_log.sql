USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.ADMIN_ACTIONS_LOG (
  LOG_ID STRING PRIMARY KEY,
  ADMIN_USER STRING,
  ACTION STRING,
  TARGET_REF STRING,
  LOGGED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP(),
  DETAILS VARIANT
);

