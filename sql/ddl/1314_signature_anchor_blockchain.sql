-- Optional table to record blockchain anchor transactions for immutable anchoring of evidence bundles. @1 @31
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.BUNDLE_BLOCKCHAIN_ANCHORS (
  ANCHOR_ID STRING PRIMARY KEY,
  BUNDLE_ID STRING,
  TX_ID STRING,
  BLOCKCHAIN STRING,
  ANCHORED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

