USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.INCIDENT_RESPONSE_RUNS_V2 (
  RUN_ID STRING PRIMARY KEY,
  PLAYBOOK_ID STRING,
  INCIDENT_ID STRING,
  STATUS STRING,
  STARTED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP(),
  ENDED_AT TIMESTAMP_LTZ,
  META VARIANT
);
-- Tracks execution of playbooks during incidents for audit & improvement @15 @29

