-- Audit trail for evidence export operations including operator and system runs. @1 @31
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.EVIDENCE_EXPORT_AUDIT (
  AUDIT_ID STRING PRIMARY KEY,
  EXPORT_ID STRING,
  OPERATOR STRING,
  STARTED_AT TIMESTAMP_LTZ,
  ENDED_AT TIMESTAMP_LTZ,
  STATUS STRING,
  DETAILS VARIANT
);

