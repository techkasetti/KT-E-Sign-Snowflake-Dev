USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.BIOMETRIC_RETRIES (
  RETRY_ID STRING PRIMARY KEY,
  SUBJECT_REF STRING,
  ATTEMPTS INT DEFAULT 0,
  LAST_ATTEMPT_AT TIMESTAMP_LTZ
);

