USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.UNLOCK_HISTORY (
  UNLOCK_ID STRING PRIMARY KEY,
  LOCK_ID STRING,
  UNLOCKED_BY STRING,
  UNLOCKED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP(),
  REASON STRING
);

