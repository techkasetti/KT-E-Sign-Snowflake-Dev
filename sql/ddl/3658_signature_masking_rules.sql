USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.MASKING_RULES (
  RULE_ID STRING PRIMARY KEY,
  NAME STRING,
  TARGET_FIELD STRING,
  MASK_EXPRESSION STRING,
  ENABLED BOOLEAN DEFAULT TRUE
);

