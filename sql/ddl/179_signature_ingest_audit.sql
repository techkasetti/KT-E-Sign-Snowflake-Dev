USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.INGEST_AUDIT (
  AUDIT_ID STRING PRIMARY KEY,
  RAW_ID STRING,
  HANDLER STRING,
  STATUS STRING,
  DETAILS VARIANT,
  PROCESSED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);
-- Ingest audit rows for webhook processing and troubleshooting @24 @31

