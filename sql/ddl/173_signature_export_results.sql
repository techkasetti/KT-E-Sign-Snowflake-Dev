USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.EXPORT_RESULTS (
  RESULT_ID STRING PRIMARY KEY,
  MANIFEST_ID STRING,
  ROW_COUNT NUMBER,
  STATUS STRING,
  COMPLETED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);
-- Tracks results of export jobs for reconciliation and audits @99 @2438

