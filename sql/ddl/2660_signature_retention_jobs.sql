USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.RETENTION_JOBS (
  JOB_ID STRING PRIMARY KEY,
  POLICY_ID STRING,
  STATUS STRING DEFAULT 'SCHEDULED',
  LAST_RUN_AT TIMESTAMP_LTZ,
  META VARIANT
);

