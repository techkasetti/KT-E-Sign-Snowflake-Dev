USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.SIGNATURE_EVENTS_RAW (
  RAW_ID STRING PRIMARY KEY,
  SOURCE_SYSTEM STRING,
  PAYLOAD VARIANT,
  RECEIVED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP(),
  HANDLED BOOLEAN DEFAULT FALSE
);
-- Raw stage table for Snowpipe ingestion of provider webhooks before UPSERT processing @236 @24

