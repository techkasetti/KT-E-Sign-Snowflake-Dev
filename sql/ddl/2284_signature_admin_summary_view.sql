CREATE OR REPLACE VIEW DOCGEN.V_ADMIN_SIGNATURE_SUMMARY AS
SELECT COUNT(DISTINCT BUNDLE_ID) AS TOTAL_BUNDLES, SUM(CASE WHEN STATUS='COMPLETED' THEN 1 ELSE 0 END) AS COMPLETED FROM DOCGEN.FINALIZED_BUNDLES;

