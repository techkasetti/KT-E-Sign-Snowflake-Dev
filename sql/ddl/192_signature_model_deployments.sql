USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.MODEL_DEPLOYMENTS (
  DEPLOY_ID STRING PRIMARY KEY,
  MODEL_ID STRING,
  ENVIRONMENT STRING,
  ROUTING_RULES VARIANT,
  DEPLOYED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);
-- Tracks model deployments and routing rules used by Admin Console for A/B or canary routing @180 @176

