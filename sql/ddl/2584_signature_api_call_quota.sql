USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.API_CALL_QUOTAS (
  QUOTA_ID STRING PRIMARY KEY,
  ACCOUNT_ID STRING,
  WINDOW_SECONDS INT,
  MAX_CALLS INT,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

