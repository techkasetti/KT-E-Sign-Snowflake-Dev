USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.FORENSICS_EXPORTS (
  EXPORT_ID STRING PRIMARY KEY,
  BUNDLE_ID STRING,
  SCOPE VARIANT,
  TARGET_STAGE STRING,
  STATUS STRING,
  REQUESTED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

