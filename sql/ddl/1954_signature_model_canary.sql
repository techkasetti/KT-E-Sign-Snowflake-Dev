USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.MODEL_CANARY_CONFIG (
  CANARY_ID STRING PRIMARY KEY,
  VERSION_ID STRING,
  PERCENT_ROLLOUT FLOAT,
  METRICS VARIANT,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

