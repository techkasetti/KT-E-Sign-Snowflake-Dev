USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.OWNER_MAPPING (
  OWNER_ID STRING PRIMARY KEY,
  ACCOUNT_ID STRING,
  CONTACT_REF STRING,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

Maps external owner to tenant/account for multi-tenant routing. @229 @31

