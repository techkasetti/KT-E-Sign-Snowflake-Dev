USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.OAUTH_TOKENS_13 (
  TOKEN_ID STRING PRIMARY KEY,
  CLIENT_ID STRING,
  SUBJECT STRING,
  SCOPE STRING,
  EXPIRES_AT TIMESTAMP_LTZ,
  ISSUED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

