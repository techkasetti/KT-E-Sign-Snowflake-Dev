USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.TEMPLATE_VERSION_LOCKS (
  LOCK_ID STRING PRIMARY KEY,
  TEMPLATE_VERSION_ID STRING,
  LOCKED_BY STRING,
  LOCKED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

