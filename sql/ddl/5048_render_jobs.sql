USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.RENDER_JOBS (
  JOB_ID STRING PRIMARY KEY,
  ASSEMBLY_ID STRING,
  STATUS STRING,
  WORKER_NODE STRING,
  ATTEMPTS INT DEFAULT 0,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

Render job tracker for assembly-to-PDF generation pipeline. @66 @31

