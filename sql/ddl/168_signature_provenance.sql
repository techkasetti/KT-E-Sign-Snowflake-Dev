-- Provenance records tying events, verification runs, and evidence bundles for auditability as described in the Evidence design. @28 @31
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.SIGNATURE_PROVENANCE (
  PROV_ID STRING PRIMARY KEY,
  REQUEST_ID STRING,
  EVENT_IDS ARRAY,
  VERIFICATION_IDS ARRAY,
  EVIDENCE_BUNDLE_ID STRING,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

