-- Per-account rate limit configuration for signing-related APIs to prevent abuse. @31 @24 @52
CREATE OR REPLACE TABLE DOCGEN.SIGNATURE_RATE_LIMITS (
  RATE_LIMIT_ID STRING PRIMARY KEY,
  ACCOUNT_ID STRING,
  LIMIT_PER_MINUTE INT,
  WINDOW_SECONDS INT,
  UPDATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

