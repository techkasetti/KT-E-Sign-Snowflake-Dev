-- Store external anchoring metadata and timestamped proof references for long-term verification. @1 @31
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.HASH_ANCHORS (
  HASH_ANCHOR_ID STRING PRIMARY KEY,
  ANCHOR_HASH STRING,
  ANCHOR_LOCATION STRING,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

