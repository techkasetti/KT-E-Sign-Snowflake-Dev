USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.CROSS_BORDER_TRANSFERS ( TRANSFER_ID STRING PRIMARY KEY, BUNDLE_ID STRING, FROM_REGION STRING, TO_REGION STRING, STATUS STRING, INITIATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP() ); @31

