-- Policy configuration for biometric use, storage, and retention to ensure privacy controls. @1 @31
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.BIOMETRIC_POLICIES (
  POLICY_ID STRING PRIMARY KEY,
  ACCOUNT_ID STRING,
  STORE_HASH_ONLY BOOLEAN DEFAULT TRUE,
  RETENTION_DAYS INT,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

