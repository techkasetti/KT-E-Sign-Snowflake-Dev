USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.SIGNATURE_CERTIFICATES (
  CERT_ID STRING PRIMARY KEY,
  SIGNER_ID STRING,
  CERT_PEM CLOB,
  FINGERPRINT STRING,
  ISSUED_AT TIMESTAMP_LTZ,
  EXPIRES_AT TIMESTAMP_LTZ
);

Certificate storage for signer certificate chains (dev/store metadata; HSM key material kept out of DB). @38 @31

