USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.SIGNER_GEO_EVENTS (
  GEO_ID STRING PRIMARY KEY,
  SIGNER_ID STRING,
  SESSION_ID STRING,
  LATITUDE NUMBER,
  LONGITUDE NUMBER,
  REPORTED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

