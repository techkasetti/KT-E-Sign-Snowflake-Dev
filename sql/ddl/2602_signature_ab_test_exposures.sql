USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.AB_TEST_EXPOSURES (
  EXPOSURE_ID STRING PRIMARY KEY,
  TEST_ID STRING,
  VARIANT_ID STRING,
  SUBJECT_REF STRING,
  EXPOSED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP(),
  META VARIANT
);

