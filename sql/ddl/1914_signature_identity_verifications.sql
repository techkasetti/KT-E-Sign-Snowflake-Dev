USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.IDENTITY_VERIFICATIONS (
  VERIF_ID STRING PRIMARY KEY,
  PROOF_ID STRING,
  VERIFIED_BY STRING,
  RESULT STRING,
  VERIFIED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

