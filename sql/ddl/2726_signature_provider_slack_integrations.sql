USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.SLACK_INTEGRATIONS (
  INTEG_ID STRING PRIMARY KEY,
  PROVIDER_ID STRING,
  WEBHOOK_URL STRING,
  CHANNEL STRING,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

