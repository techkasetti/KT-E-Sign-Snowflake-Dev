USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.SIGNATURE_VERIFICATIONS (
  VERIFY_ID STRING PRIMARY KEY,
  BUNDLE_ID STRING,
  SIGNER_ID STRING,
  VERIFIED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP(),
  METHOD STRING,
  RESULT STRING,
  DETAILS VARIANT
);

Records of verification runs, OCSP/CRL checks, and results for compliance. @94 @31

