USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.RECONCILIATION_JOBS (
  JOB_ID STRING PRIMARY KEY,
  TARGET_TABLE STRING,
  STATUS STRING DEFAULT 'PENDING',
  STARTED_AT TIMESTAMP_LTZ,
  ENDED_AT TIMESTAMP_LTZ,
  META VARIANT
);
-- Reconciliation job registry for billing/evidence/exports to ensure determinism @66 @71

