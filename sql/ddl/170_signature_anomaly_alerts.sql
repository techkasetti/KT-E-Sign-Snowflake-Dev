USE DATABASE AI_FEATURE_HUB; USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.ANOMALY_ALERTS (
  ALERT_ID STRING PRIMARY KEY,
  ENTITY_REF STRING,
  ALERT_TYPE STRING,
  SCORE NUMBER,
  DETAILS VARIANT,
  CREATED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);
-- Alerts table for anomalies detected in signing behavior or system metrics @236 @356

