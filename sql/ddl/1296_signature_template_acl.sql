-- Per-template access control list for template editing/usage (separate from row-level policies). @1 @31
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE TABLE DOCGEN.SIGNATURE_TEMPLATE_ACL (
  ACL_ID STRING PRIMARY KEY,
  TEMPLATE_ID STRING,
  PRINCIPAL STRING,
  ROLE STRING,
  GRANTED_AT TIMESTAMP_LTZ DEFAULT CURRENT_TIMESTAMP()
);

