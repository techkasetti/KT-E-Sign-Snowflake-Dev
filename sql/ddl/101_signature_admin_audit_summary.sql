USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE MATERIALIZED VIEW DOCGEN.MV_ADMIN_AUDIT_ACTIVITY AS
SELECT at.AUDIT_ID, at.ADMIN_USER, at.ACTION, at.TARGET, at.AUDIT_TS
FROM DOCGEN.ADMIN_AUDIT at
ORDER BY at.AUDIT_TS DESC; Generated per Snowflake E-Sign patterns. @124 @31

