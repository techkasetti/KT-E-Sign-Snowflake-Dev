```sql
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE VIEW DOCGEN.V_REPORT_RUNS_RECENT AS
SELECT RUN_ID, TEMPLATE_ID, STATUS, STARTED_AT, COMPLETED_AT FROM DOCGEN.REPORT_RUNS ORDER BY STARTED_AT DESC LIMIT 1000;
```

