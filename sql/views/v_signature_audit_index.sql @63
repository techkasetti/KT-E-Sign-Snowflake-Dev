```sql
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE VIEW DOCGEN.V_SIGNATURE_AUDIT_INDEX AS
SELECT SAI_ID, SIGNATURE_ID, BUNDLE_ID, SIGNER_ID, AUDIT_HASH, INDEXED_AT FROM DOCGEN.SIGNATURE_AUDIT_INDEX ORDER BY INDEXED_AT DESC;
```

