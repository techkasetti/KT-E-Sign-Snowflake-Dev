```sql
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE VIEW DOCGEN.V_PROVENANCE_LINKS AS
SELECT PL_ID, PARENT_REF, CHILD_REF, RELATION_TYPE, LINKED_AT FROM DOCGEN.PROVENANCE_LINKS ORDER BY LINKED_AT DESC;
```

