```sql
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE VIEW DOCGEN.V_AUDIT_AGGREGATE_LATEST AS
SELECT AGG_ID, AGG_KEY, AGG_PAYLOAD, AGG_AT FROM DOCGEN.AUDIT_AGGREGATES ORDER BY AGG_AT DESC LIMIT 1;
```

