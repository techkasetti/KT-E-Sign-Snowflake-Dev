```sql
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE VIEW DOCGEN.V_QUARANTINE_STORE AS
SELECT Q_ID, BUNDLE_ID, REASON, CREATED_AT FROM DOCGEN.QUARANTINE_STORE ORDER BY CREATED_AT DESC;
```

