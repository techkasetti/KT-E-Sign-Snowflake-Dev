```sql
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE VIEW DOCGEN.V_ANALYTICS_EVENTS_RECENT AS
SELECT EVT_ID, EVENT_NAME, OCCURRED_AT FROM DOCGEN.ANALYTICS_EVENTS ORDER BY OCCURRED_AT DESC LIMIT 1000;
```

