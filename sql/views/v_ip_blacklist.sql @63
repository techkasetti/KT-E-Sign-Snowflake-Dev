```sql
USE DATABASE AI_FEATURE_HUB;
USE SCHEMA DOCGEN;
CREATE OR REPLACE VIEW DOCGEN.V_IP_BLACKLIST AS
SELECT IP_ID, IP_ADDRESS, REASON, BLOCKED_AT FROM DOCGEN.IP_BLACKLIST ORDER BY BLOCKED_AT DESC;
```

