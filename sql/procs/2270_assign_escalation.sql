CREATE OR REPLACE PROCEDURE DOCGEN.ASSIGN_ESCALATION(esc_id STRING, assignee STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ESCALATION_ASSIGNMENTS (ASSIGN_ID, ESC_ID, ASSIGNEE) VALUES (UUID_STRING(), :esc_id, :assignee);
$$

