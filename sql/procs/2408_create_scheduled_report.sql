CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_SCHEDULED_REPORT(name STRING, definition VARIANT, schedule VARIANT, owner STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SCHEDULED_REPORTS (SREP_ID, NAME, DEFINITION, SCHEDULE, OWNER) VALUES (UUID_STRING(), :name, :definition, :schedule, :owner);
$$

