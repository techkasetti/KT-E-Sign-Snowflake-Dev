CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_ALERT_DISPATCH(rule_id STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ALERT_DISPATCH_QUEUE (Q_ID, RULE_ID, PAYLOAD) VALUES (UUID_STRING(), :rule_id, :payload);
$$
