CREATE OR REPLACE PROCEDURE DOCGEN.TAG_TEMPLATE(template_id STRING, tag STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TEMPLATE_TAGS (TAG_ID, TEMPLATE_ID, TAG) VALUES (UUID_STRING(), :template_id, :tag);
$$;

