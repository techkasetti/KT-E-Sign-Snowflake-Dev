CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_POLICY_TEMPLATE(template_id STRING, name STRING, body CLOB, created_by STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.POLICY_TEMPLATES (TEMPLATE_ID, NAME, BODY, CREATED_BY) VALUES (:template_id, :name, :body, :created_by);
$$

