CREATE OR REPLACE PROCEDURE DOCGEN.BIND_PROVIDER_POLICY(provider_id STRING, policy_id STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PROVIDER_POLICY_BINDINGS (BIND_ID, PROVIDER_ID, POLICY_ID) VALUES (UUID_STRING(), :provider_id, :policy_id);
$$

