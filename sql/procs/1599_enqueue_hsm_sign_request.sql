CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_HSM_SIGN_REQUEST(bundle_id STRING, method STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.HSM_SIGN_REQUESTS (REQ_ID, BUNDLE_ID, SIGNATURE_METHOD) VALUES (UUID_STRING(), :bundle_id, :method);
$$;

