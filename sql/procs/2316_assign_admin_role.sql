CREATE OR REPLACE PROCEDURE DOCGEN.ASSIGN_ADMIN_ROLE(role_id STRING, user_id STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ADMIN_ROLE_ASSIGNMENTS (ASSIGN_ID, ROLE_ID, USER_ID) VALUES (UUID_STRING(), :role_id, :user_id);
$$

