CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_REPROCESS(target_ref STRING, reason STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.REPROCESS_QUEUE (QUEUE_ID, TARGET_REF, REASON) VALUES (UUID_STRING(), :target_ref, :reason);
$$

