CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_APPROVAL_WORKFLOW(name STRING, steps VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.APPROVAL_WORKFLOWS (WF_ID, NAME, STEPS)
VALUES (UUID_STRING(), :name, :steps);
$$;

