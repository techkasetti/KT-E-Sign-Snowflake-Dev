CREATE OR REPLACE PROCEDURE DOCGEN.EMIT_INTEGRATION_EVENT(integration STRING, event_type STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INTEGRATION_EVENTS (EVENT_ID, INTEGRATION, EVENT_TYPE, PAYLOAD) VALUES (UUID_STRING(), :integration, :event_type, :payload);
$$

