CREATE OR REPLACE PROCEDURE DOCGEN.SEND_ADMIN_NOTIFICATION(target_role STRING, message STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ADMIN_NOTIFICATIONS (NOTIF_ID, TARGET_ROLE, MESSAGE, META) VALUES (UUID_STRING(), :target_role, :message, :meta);
$$

