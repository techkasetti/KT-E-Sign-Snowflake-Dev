CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_AUDIT_SNAPSHOT_MANIFEST(scope VARIANT, files ARRAY)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.AUDIT_SNAPSHOT_MANIFEST (MANIFEST_ID, SCOPE, FILES) VALUES (UUID_STRING(), :scope, :files);
$$

