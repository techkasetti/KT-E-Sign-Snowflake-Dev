CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_SIGNATURE_ACK_01(bundle_id STRING, signer_ref STRING, method STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SIGNATURE_ACKNOWLEDGEMENTS_01 (ACK_ID, BUNDLE_ID, SIGNER_REF, METHOD, META)
VALUES (UUID_STRING(), :bundle_id, :signer_ref, :method, :meta);
$$

