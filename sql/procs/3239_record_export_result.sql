CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_EXPORT_RESULT(export_id STRING, status STRING, location STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.EXPORT_RESULTS (RESULT_ID, EXPORT_ID, STATUS, LOCATION) VALUES (UUID_STRING(), :export_id, :status, :location);
$$

