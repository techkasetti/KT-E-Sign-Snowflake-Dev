CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_HSM_RESPONSE(req_id STRING, response VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.HSM_RESPONSES (RESP_ID, REQ_ID, RESPONSE) VALUES (UUID_STRING(), :req_id, :response);
$$

