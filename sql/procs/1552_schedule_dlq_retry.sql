CREATE OR REPLACE PROCEDURE DOCGEN.SCHEDULE_DLQ_RETRY(dlq_id STRING, delay_seconds INT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DLQ_RETRY_QUEUE (RETRY_ID, DLQ_ID, NEXT_ATTEMPT_AT) VALUES (UUID_STRING(), :dlq_id, DATEADD(second, :delay_seconds, CURRENT_TIMESTAMP()));
$$;

