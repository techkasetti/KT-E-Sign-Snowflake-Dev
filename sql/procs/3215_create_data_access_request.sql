CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_DATA_ACCESS_REQUEST(requestor STRING, target_ref STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DATA_ACCESS_REQUESTS (REQ_ID, REQUESTOR, TARGET_REF) VALUES (UUID_STRING(), :requestor, :target_ref);
$$

