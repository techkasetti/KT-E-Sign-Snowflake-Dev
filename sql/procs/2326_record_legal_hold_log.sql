CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_LEGAL_HOLD_LOG(hold_id STRING, action STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.LEGAL_HOLD_LOGS (LOG_ID, HOLD_ID, ACTION, DETAILS) VALUES (UUID_STRING(), :hold_id, :action, :details);
$$;

