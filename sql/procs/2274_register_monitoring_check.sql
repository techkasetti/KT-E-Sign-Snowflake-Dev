CREATE OR REPLACE PROCEDURE DOCGEN.REGISTER_MONITORING_CHECK(name STRING, type STRING, config VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.MONITORING_CHECKS (CHECK_ID, NAME, TYPE, CONFIG) VALUES (UUID_STRING(), :name, :type, :config);
$$

