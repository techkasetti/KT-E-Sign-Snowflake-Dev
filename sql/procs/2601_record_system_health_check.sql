CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_SYSTEM_HEALTH_CHECK(component STRING, status STRING, metrics VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SYSTEM_HEALTH_CHECKS (HC_ID, COMPONENT, STATUS, METRICS) VALUES (UUID_STRING(), :component, :status, :metrics);
$$

