CREATE OR REPLACE PROCEDURE DOCGEN.APPROVE_KEY_ROTATION(req_id STRING, approved_by STRING, notes STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.KEY_ROTATION_APPROVALS (APPROVAL_ID, REQ_ID, APPROVED_BY, NOTES) VALUES (UUID_STRING(), :req_id, :approved_by, :notes);
$$

