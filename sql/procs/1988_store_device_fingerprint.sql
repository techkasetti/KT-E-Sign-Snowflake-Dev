CREATE OR REPLACE PROCEDURE DOCGEN.STORE_DEVICE_FINGERPRINT(signer_id STRING, fingerprint STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DEVICE_FINGERPRINTS (FP_ID, SIGNER_ID, FINGERPRINT, META) VALUES (UUID_STRING(), :signer_id, :fingerprint, :meta);
$$;

