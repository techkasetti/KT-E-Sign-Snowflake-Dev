CREATE OR REPLACE PROCEDURE DOCGEN.SCHEDULE_WEBHOOK_BACKOFF_13(backoff_id STRING, event_id STRING, next_attempt_at TIMESTAMP_LTZ, backoff_ms NUMBER)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SIG_WEBHOOK_BACKOFF_13 (BACKOFF_ID, EVENT_ID, NEXT_ATTEMPT_AT, BACKOFF_MS) VALUES (:backoff_id, :event_id, :next_attempt_at, :backoff_ms);
$$

