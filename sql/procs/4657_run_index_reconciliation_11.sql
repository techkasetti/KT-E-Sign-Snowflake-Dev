CREATE OR REPLACE PROCEDURE DOCGEN.RUN_INDEX_RECONCILIATION_11(recon_id STRING, shard_id STRING, expected_count NUMBER, actual_count NUMBER, status STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INDEX_RECONCILIATION_11 (RECON_ID, SHARD_ID, EXPECTED_COUNT, ACTUAL_COUNT, STATUS) VALUES (:recon_id, :shard_id, :expected_count, :actual_count, :status);
$$

