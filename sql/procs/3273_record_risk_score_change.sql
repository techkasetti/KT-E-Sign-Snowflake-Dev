CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_RISK_SCORE_CHANGE(score_id STRING, prev_score FLOAT, new_score FLOAT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RISK_SCORE_HISTORY (HIST_ID, SCORE_ID, PREV_SCORE, NEW_SCORE) VALUES (UUID_STRING(), :score_id, :prev_score, :new_score);
$$

