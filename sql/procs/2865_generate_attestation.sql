CREATE OR REPLACE PROCEDURE DOCGEN.GENERATE_ATTESTATION(bundle_id STRING, template_id STRING, content CLOB)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ATTESTATIONS (ATTEST_ID, BUNDLE_ID, TEMPLATE_ID, CONTENT) VALUES (UUID_STRING(), :bundle_id, :template_id, :content);
$$

