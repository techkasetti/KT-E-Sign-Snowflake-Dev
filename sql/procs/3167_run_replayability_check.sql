CREATE OR REPLACE PROCEDURE DOCGEN.RUN_REPLAYABILITY_CHECK(bundle_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.REPLAYABILITY_CHECKS (CHECK_ID, BUNDLE_ID, STATUS, DETAILS) VALUES (UUID_STRING(), :bundle_id, :status, :details);
$$

