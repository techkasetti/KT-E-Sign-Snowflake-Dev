CREATE OR REPLACE PROCEDURE DOCGEN.RAISE_COMPLIANCE_FLAG_04(flag_id STRING, target_ref STRING, flag_type STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.COMPLIANCE_FLAGS_04 (FLAG_ID, TARGET_REF, FLAG_TYPE, DETAILS) VALUES (:flag_id, :target_ref, :flag_type, :details);
$$

