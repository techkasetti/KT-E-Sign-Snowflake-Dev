CREATE OR REPLACE PROCEDURE DOCGEN.QUARANTINE_PAYLOAD(reason STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.QUARANTINE_BLOBS (QUARANTINE_ID, REASON, PAYLOAD) VALUES (UUID_STRING(), :reason, :payload);
$$;

