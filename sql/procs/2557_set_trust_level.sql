CREATE OR REPLACE PROCEDURE DOCGEN.SET_TRUST_LEVEL(subject_ref STRING, level STRING, source STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TRUST_LEVELS (TL_ID, SUBJECT_REF, LEVEL, SOURCE) VALUES (UUID_STRING(), :subject_ref, :level, :source);
$$

