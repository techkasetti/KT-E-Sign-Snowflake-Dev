CREATE OR REPLACE PROCEDURE DOCGEN.MARK_TEMPLATE_OBSOLETE(template_id STRING, reason STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TEMPLATE_OBSOLESCENCE (OBS_ID, TEMPLATE_ID, REASON) VALUES (UUID_STRING(), :template_id, :reason);
$$
