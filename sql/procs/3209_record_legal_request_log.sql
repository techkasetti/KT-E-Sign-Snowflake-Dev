CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_LEGAL_REQUEST_LOG(request_id STRING, status STRING, notes STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.LEGAL_REQUEST_LOGS (LOG_ID, REQUEST_ID, STATUS, NOTES) VALUES (UUID_STRING(), :request_id, :status, :notes);
$$

