CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_LEGAL_HOLD_EXPORT(hold_id STRING, stage_location STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.LEGAL_HOLD_EXPORTS (EXPORT_ID, HOLD_ID, STAGE_LOCATION) VALUES (UUID_STRING(), :hold_id, :stage_location);
$$

