CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_IDENTITY_VERIFICATION(proof_id STRING, verified_by STRING, result STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.IDENTITY_VERIFICATIONS (VERIF_ID, PROOF_ID, VERIFIED_BY, RESULT)
VALUES (UUID_STRING(), :proof_id, :verified_by, :result);
$$;

