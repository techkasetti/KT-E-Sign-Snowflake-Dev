CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_SMOKE_RESULT(test_id STRING, status STRING, output VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SMOKE_RESULTS (RESULT_ID, TEST_ID, STATUS, OUTPUT) VALUES (UUID_STRING(), :test_id, :status, :output);
$$

