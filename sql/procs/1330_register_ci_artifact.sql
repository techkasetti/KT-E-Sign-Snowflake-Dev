-- Register a CI artifact for tracking by CI pipelines. @1 @31
CREATE OR REPLACE PROCEDURE DOCGEN.REGISTER_CI_ARTIFACT(name STRING, description STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.CI_ARTIFACTS_REGISTRY (ARTIFACT_ID, NAME, DESCRIPTION) VALUES (UUID_STRING(), :name, :description);
$$;

