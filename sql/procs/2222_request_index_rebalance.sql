CREATE OR REPLACE PROCEDURE DOCGEN.REQUEST_INDEX_REBALANCE(index_name STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INDEX_REBALANCE_OPS (OP_ID, INDEX_NAME, DETAILS) VALUES (UUID_STRING(), :index_name, :details);
$$

