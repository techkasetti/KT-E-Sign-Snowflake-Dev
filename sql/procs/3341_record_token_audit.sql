CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_TOKEN_AUDIT(token_id STRING, action STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TOKEN_AUDIT (TA_ID, TOKEN_ID, ACTION, DETAILS) VALUES (UUID_STRING(), :token_id, :action, :details);
$$

