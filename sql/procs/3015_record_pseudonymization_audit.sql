CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_PSEUDONYMIZATION_AUDIT(map_id STRING, action STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PSEUDONYMIZATION_AUDIT (AUDIT_ID, MAP_ID, ACTION, DETAILS) VALUES (UUID_STRING(), :map_id, :action, :details);
$$

