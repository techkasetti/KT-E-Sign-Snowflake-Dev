CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_REQUEST_GEODATA(request_id STRING, ip_address STRING, geo JSON)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.REQUEST_GEODATA (GEODATA_ID, REQUEST_ID, IP_ADDRESS, GEO) VALUES (UUID_STRING(), :request_id, :ip_address, :geo);
$$;

