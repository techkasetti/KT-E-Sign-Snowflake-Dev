CREATE OR REPLACE PROCEDURE DOCGEN.STORE_SIGNATURE_IMAGE(signer_id STRING, image_ref STRING, hash STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SIGNATURE_IMAGES (IMAGE_ID, SIGNER_ID, IMAGE_REF, HASH) VALUES (UUID_STRING(), :signer_id, :image_ref, :hash);
$$;

