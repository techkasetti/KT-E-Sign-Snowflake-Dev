CREATE OR REPLACE PROCEDURE DOCGEN.STORE_PROVIDER_API_KEY(key_id STRING, provider_name STRING, key_hash STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PROVIDER_API_KEYS (KEY_ID, PROVIDER_NAME, KEY_HASH) VALUES (:key_id, :provider_name, :key_hash);
$$

