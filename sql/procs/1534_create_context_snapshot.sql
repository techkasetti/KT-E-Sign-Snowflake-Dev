CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_CONTEXT_SNAPSHOT(request_id STRING, snapshot VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.CONTEXT_SNAPSHOTS (SNAP_ID, REQUEST_ID, SNAPSHOT) VALUES (UUID_STRING(), :request_id, :snapshot);
$$;

