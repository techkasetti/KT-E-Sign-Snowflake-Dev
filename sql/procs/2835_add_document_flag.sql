CREATE OR REPLACE PROCEDURE DOCGEN.ADD_DOCUMENT_FLAG(document_id STRING, flag_type STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DOCUMENT_FLAGS (FLAG_ID, DOCUMENT_ID, FLAG_TYPE, META) VALUES (UUID_STRING(), :document_id, :flag_type, :meta);
$$

