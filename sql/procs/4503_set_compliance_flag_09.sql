CREATE OR REPLACE PROCEDURE DOCGEN.SET_COMPLIANCE_FLAG_09(flag_id STRING, bundle_id STRING, flag_key STRING, flag_value VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.COMPLIANCE_FLAGS_09 (FLAG_ID, BUNDLE_ID, FLAG_KEY, FLAG_VALUE) VALUES (:flag_id, :bundle_id, :flag_key, :flag_value);
$$

