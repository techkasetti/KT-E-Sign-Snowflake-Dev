CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_USER_CONSENT(subject_ref STRING, consent_type STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.USER_CONSENT_RECORDS (CONSENT_ID, SUBJECT_REF, CONSENT_TYPE, META) VALUES (UUID_STRING(), :subject_ref, :consent_type, :meta);
$$

