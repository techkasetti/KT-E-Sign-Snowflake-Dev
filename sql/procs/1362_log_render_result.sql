CREATE OR REPLACE PROCEDURE DOCGEN.LOG_RENDER_RESULT(render_id STRING, template_id STRING, assembly_id STRING, rendered_url STRING, status STRING, log VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$ INSERT INTO DOCGEN.TEMPLATE_RENDER_LOGS (RENDER_ID, TEMPLATE_ID, ASSEMBLY_ID, RENDERED_URL, STATUS, LOG) VALUES (UUID_STRING(), :template_id, :assembly_id, :rendered_url, :status, :log); $$;

