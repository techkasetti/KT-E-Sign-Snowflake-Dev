CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_USER_ACTIVITY(user_id STRING, action STRING, entity_ref STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.USER_ACTIVITY (ACTIVITY_ID, USER_ID, ACTION, ENTITY_REF) VALUES (UUID_STRING(), :user_id, :action, :entity_ref);
$$;

