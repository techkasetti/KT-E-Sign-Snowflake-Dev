CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_OPERATOR_ESCALATION_POLICY(name STRING, steps VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.OPERATOR_ESCALATION_POLICIES (POLICY_ID, NAME, STEPS) VALUES (UUID_STRING(), :name, :steps);
$$

