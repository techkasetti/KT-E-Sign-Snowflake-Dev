CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_ADMIN_APPROVAL_ACTION(approval_id STRING, action_by STRING, action STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ADMIN_APPROVAL_ACTIONS (ACTION_ID, APPROVAL_ID, ACTION_BY, ACTION) VALUES (UUID_STRING(), :approval_id, :action_by, :action);
$$;

