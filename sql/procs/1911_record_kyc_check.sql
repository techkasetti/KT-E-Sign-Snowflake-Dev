CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_KYC_CHECK(signer_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.KYC_CHECKS (KYC_ID, SIGNER_ID, STATUS, DETAILS)
VALUES (UUID_STRING(), :signer_id, :status, :details);
$$;

