CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_ACCESS_REVIEW(target_ref STRING, reviewer STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ACCESS_REVIEW_QUEUE (QUEUE_ID, TARGET_REF, REVIEWER) VALUES (UUID_STRING(), :target_ref, :reviewer);
$$;

