CREATE OR REPLACE PROCEDURE DOCGEN.ADD_TEMPLATE_RETENTION_EXEMPTION(template_id STRING, reason STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TEMPLATE_RETENTION_EXEMPTIONS (EXEMPT_ID, TEMPLATE_ID, REASON) VALUES (UUID_STRING(), :template_id, :reason);
$$
