CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_POLICY_EVALUATOR_RUN(eval_id STRING, context VARIANT, result VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.POLICY_EVALUATOR_RUNS (RUN_ID, EVAL_ID, CONTEXT, RESULT) VALUES (UUID_STRING(), :eval_id, :context, :result);
$$

