CREATE OR REPLACE PROCEDURE DOCGEN.OPEN_DISPUTE(refund_id STRING, account_id STRING, reason STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DISPUTES (DISPUTE_ID, REFUND_ID, ACCOUNT_ID, REASON, META)
VALUES (UUID_STRING(), :refund_id, :account_id, :reason, :meta);
$$

