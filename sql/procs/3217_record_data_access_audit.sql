CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_DATA_ACCESS_AUDIT(req_id STRING, decision STRING, decided_by STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DATA_ACCESS_AUDIT (AUDIT_ID, REQ_ID, DECISION, DECIDED_BY) VALUES (UUID_STRING(), :req_id, :decision, :decided_by);
$$

