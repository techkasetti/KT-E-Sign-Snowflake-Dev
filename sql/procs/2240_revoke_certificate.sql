CREATE OR REPLACE PROCEDURE DOCGEN.REVOKE_CERTIFICATE(cert_id STRING, reason STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.CERTIFICATE_REVOCATIONS (REVOC_ID, CERT_ID, REASON) VALUES (UUID_STRING(), :cert_id, :reason);
$$

