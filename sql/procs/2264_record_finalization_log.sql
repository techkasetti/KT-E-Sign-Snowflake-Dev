CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_FINALIZATION_LOG(fq_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.FINALIZATION_LOGS (LOG_ID, FQ_ID, STATUS, DETAILS) VALUES (UUID_STRING(), :fq_id, :status, :details);
$$

