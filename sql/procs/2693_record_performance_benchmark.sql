CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_PERFORMANCE_BENCHMARK(name STRING, metrics VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PERFORMANCE_BENCHMARKS (BENCH_ID, NAME, METRICS) VALUES (UUID_STRING(), :name, :metrics);
$$

