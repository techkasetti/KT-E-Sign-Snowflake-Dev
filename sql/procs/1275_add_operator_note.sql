-- Add an operator note for a request. @31 @24 @52
CREATE OR REPLACE PROCEDURE DOCGEN.ADD_OPERATOR_NOTE(request_id STRING, operator STRING, note TEXT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$ INSERT INTO DOCGEN.SIGNATURE_OPERATOR_NOTES (NOTE_ID, REQUEST_ID, OPERATOR, NOTE) VALUES (UUID_STRING(), :request_id, :operator, :note); $$;

