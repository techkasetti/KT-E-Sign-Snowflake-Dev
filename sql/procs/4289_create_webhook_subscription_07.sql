CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_WEBHOOK_SUBSCRIPTION_07(sub_id STRING, account_id STRING, target_url STRING, events ARRAY, secret_hash STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.WEBHOOK_SUBSCRIPTIONS_07 (SUB_ID, ACCOUNT_ID, TARGET_URL, EVENTS, SECRET_HASH) VALUES (:sub_id, :account_id, :target_url, :events, :secret_hash);
$$

