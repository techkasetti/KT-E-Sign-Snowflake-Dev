CREATE OR REPLACE PROCEDURE DOCGEN.STORE_IDENTITY_PROOF(signer_id STRING, proof_type STRING, proof_data VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.IDENTITY_PROOFS (PROOF_ID, SIGNER_ID, PROOF_TYPE, PROOF_DATA)
VALUES (UUID_STRING(), :signer_id, :proof_type, :proof_data);
$$;

