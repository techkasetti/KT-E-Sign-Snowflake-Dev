CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_REDACTION_LOG(req_id STRING, status STRING, output_url STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.REDACTION_LOGS (LOG_ID, REQ_ID, STATUS, OUTPUT_URL) VALUES (UUID_STRING(), :req_id, :status, :output_url);
$$

