CREATE OR REPLACE PROCEDURE DOCGEN.REVOKE_SESSION(session_id STRING, reason STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SESSION_REVOCATIONS (RV_ID, SESSION_ID, REASON) VALUES (UUID_STRING(), :session_id, :reason);
$$

