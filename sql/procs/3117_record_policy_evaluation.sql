CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_POLICY_EVALUATION(rule_id STRING, subject_ref STRING, result VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.POLICY_EVALUATIONS (EVAL_ID, RULE_ID, SUBJECT_REF, RESULT) VALUES (UUID_STRING(), :rule_id, :subject_ref, :result);
$$

