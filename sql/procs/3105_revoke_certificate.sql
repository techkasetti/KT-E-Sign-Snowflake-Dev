CREATE OR REPLACE PROCEDURE DOCGEN.REVOKE_CERTIFICATE(cert_id STRING, revoked_reason STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.CERTIFICATE_REVOCATION_LIST (CRL_ID, CERT_ID, REVOKED_REASON) VALUES (UUID_STRING(), :cert_id, :revoked_reason);
$$

