CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_REDACTION_RULE(name STRING, pattern STRING, scope VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.REDACTION_RULES (RULE_ID, NAME, PATTERN, SCOPE)
VALUES (UUID_STRING(), :name, :pattern, :scope);
$$;

