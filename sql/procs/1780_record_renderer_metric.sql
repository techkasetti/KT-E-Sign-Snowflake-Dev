CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_RENDERER_METRIC(renderer_id STRING, name STRING, value VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RENDERER_METRICS (METRIC_ID, RENDERER_ID, METRIC_NAME, METRIC_VALUE) VALUES (UUID_STRING(), :renderer_id, :name, :value);
$$;

