CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_DEPENDENCY_CYCLE(template_ids ARRAY)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TEMPLATE_DEPENDENCY_CYCLES (CYCLE_ID, TEMPLATE_IDS) VALUES (UUID_STRING(), :template_ids);
$$
