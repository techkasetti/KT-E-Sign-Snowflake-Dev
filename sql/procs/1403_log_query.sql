CREATE OR REPLACE PROCEDURE DOCGEN.LOG_QUERY(query_text STRING, user_id STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$ INSERT INTO DOCGEN.QUERY_LOGS (QUERY_ID, QUERY_TEXT, USER_ID) VALUES (UUID_STRING(), :query_text, :user_id); $$;

