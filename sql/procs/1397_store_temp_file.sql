CREATE OR REPLACE PROCEDURE DOCGEN.STORE_TEMP_FILE(path STRING, metadata VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$ INSERT INTO DOCGEN.TEMP_FILES (TEMP_ID, PATH, METADATA) VALUES (UUID_STRING(), :path, :metadata); $$;

