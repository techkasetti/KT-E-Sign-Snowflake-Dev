CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_SEARCH_BACKFILL(index_name STRING, range VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SEARCH_BACKFILL_QUEUE (BACKFILL_ID, INDEX_NAME, RANGE) VALUES (UUID_STRING(), :index_name, :range);
$$

