CREATE OR REPLACE PROCEDURE DOCGEN.STORE_DEVICE_FINGERPRINT(subject_ref STRING, fingerprint VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DEVICE_FINGERPRINTS (FINGER_ID, SUBJECT_REF, FINGERPRINT) VALUES (UUID_STRING(), :subject_ref, :fingerprint);
$$

