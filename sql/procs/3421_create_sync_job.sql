CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_SYNC_JOB(object_name STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SYNC_JOBS (JOB_ID, OBJECT_NAME, META) VALUES (UUID_STRING(), :object_name, :meta);
$$

