CREATE OR REPLACE PROCEDURE DOCGEN.LINK_ARCHIVAL_AUDIT(manifest_id STRING, archive_stage STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ARCHIVAL_AUDIT_INDEX (INDEX_ID, MANIFEST_ID, ARCHIVE_STAGE) VALUES (UUID_STRING(), :manifest_id, :archive_stage);
$$;

