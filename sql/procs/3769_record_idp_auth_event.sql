CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_IDP_AUTH_EVENT(idp_id STRING, user_ref STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.IDP_AUTH_EVENTS (EVENT_ID, IDP_ID, USER_REF, EVENT_PAYLOAD) VALUES (UUID_STRING(), :idp_id, :user_ref, :payload);
$$

