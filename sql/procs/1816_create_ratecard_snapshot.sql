CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_RATECARD_SNAPSHOT(account_id STRING, snapshot VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RATECARD_SNAPSHOTS (SNAP_ID, ACCOUNT_ID, SNAPSHOT) VALUES (UUID_STRING(), :account_id, :snapshot);
$$;

