CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_BIOMETRIC_EVENT_11(event_id STRING, signer_id STRING, bundle_id STRING, event_type STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.BIOMETRIC_EVENTS_11 (EVENT_ID, SIGNER_ID, BUNDLE_ID, EVENT_TYPE, PAYLOAD) VALUES (:event_id, :signer_id, :bundle_id, :event_type, :payload);
$$

