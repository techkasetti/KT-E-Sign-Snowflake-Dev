CREATE OR REPLACE PROCEDURE DOCGEN.SCHEDULE_MV_REFRESH(mv_id STRING, cron_expr STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.MV_REFRESH_JOBS (JOB_ID, MV_ID, SCHEDULE_CRON) VALUES (UUID_STRING(), :mv_id, :cron_expr);
$$;

