CREATE OR REPLACE PROCEDURE DOCGEN.STORE_SESSION_TOKEN_10(token_id STRING, signer_id STRING, token STRING, expires_at TIMESTAMP_LTZ)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SESSION_TOKENS_10 (TOKEN_ID, SIGNER_ID, TOKEN, EXPIRES_AT) VALUES (:token_id, :signer_id, :token, :expires_at);
$$

