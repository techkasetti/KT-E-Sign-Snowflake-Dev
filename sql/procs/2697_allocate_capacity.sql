CREATE OR REPLACE PROCEDURE DOCGEN.ALLOCATE_CAPACITY(plan_id STRING, account_id STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.CAPACITY_ALLOCATIONS (ALLOC_ID, PLAN_ID, ACCOUNT_ID, META) VALUES (UUID_STRING(), :plan_id, :account_id, :meta);
$$

