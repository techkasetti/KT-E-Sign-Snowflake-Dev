CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_PII_SCAN_RESULT(job_id STRING, findings VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PII_SCAN_RESULTS (RESULT_ID, JOB_ID, FINDINGS) VALUES (UUID_STRING(), :job_id, :findings);
$$

