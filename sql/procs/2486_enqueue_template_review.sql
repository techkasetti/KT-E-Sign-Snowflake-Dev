CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_TEMPLATE_REVIEW(template_id STRING, assigned_to STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TEMPLATE_REVIEW_QUEUE (Q_ID, TEMPLATE_ID, ASSIGNED_TO) VALUES (UUID_STRING(), :template_id, :assigned_to);
$$;

