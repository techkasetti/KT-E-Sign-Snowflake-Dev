CREATE OR REPLACE PROCEDURE DOCGEN.CLEANUP_ORPHANED_RECORDS()
RETURNS VARIANT
LANGUAGE SQL
AS
$$
DELETE FROM DOCGEN.SIGNATURE_IMAGES WHERE IMAGE_ID NOT IN (SELECT DISTINCT IMAGE_ID FROM DOCGEN.SIGNATURE_EVIDENCE_BUNDLE);
$$;

Cleanup for orphaned binary references after archival. @36 @31

