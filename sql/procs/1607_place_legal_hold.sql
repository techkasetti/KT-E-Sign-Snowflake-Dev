CREATE OR REPLACE PROCEDURE DOCGEN.PLACE_LEGAL_HOLD(request_id STRING, reason STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.LEGAL_HOLDS (HOLD_ID, REQUEST_ID, REASON) VALUES (UUID_STRING(), :request_id, :reason);
$$;

