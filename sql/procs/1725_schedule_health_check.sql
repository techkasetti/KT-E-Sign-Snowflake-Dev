CREATE OR REPLACE PROCEDURE DOCGEN.SCHEDULE_HEALTH_CHECK(component STRING, cron_expr STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SCHEDULED_HEALTH_CHECKS (CHECK_ID, COMPONENT, SCHEDULE_CRON) VALUES (UUID_STRING(), :component, :cron_expr);
$$; 
-- @31 @24 @52

