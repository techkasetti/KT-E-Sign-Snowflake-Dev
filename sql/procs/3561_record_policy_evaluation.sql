CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_POLICY_EVALUATION(bind_id STRING, subject_ref STRING, result VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.POLICY_EVALUATIONS (EVAL_ID, BIND_ID, SUBJECT_REF, RESULT) VALUES (UUID_STRING(), :bind_id, :subject_ref, :result);
$$

