CREATE OR REPLACE PROCEDURE DOCGEN.MAP_WEBHOOK_TO_SIGNER(raw_id STRING, signer_id STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.WEBHOOK_SIGNER_MAP (MAP_ID, RAW_ID, SIGNER_ID) VALUES (UUID_STRING(), :raw_id, :signer_id);
$$;

