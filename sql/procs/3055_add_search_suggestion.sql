CREATE OR REPLACE PROCEDURE DOCGEN.ADD_SEARCH_SUGGESTION(term STRING, score FLOAT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SEARCH_SUGGESTIONS (SUGG_ID, TERM, SCORE) VALUES (UUID_STRING(), :term, :score);
$$

