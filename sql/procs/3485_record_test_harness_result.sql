CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_TEST_HARNESS_RESULT(job_id STRING, status STRING, output VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TEST_HARNESS_RESULTS (RESULT_ID, JOB_ID, STATUS, OUTPUT) VALUES (UUID_STRING(), :job_id, :status, :output);
$$

