CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_DEMO_METRIC(name STRING, value VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DEMO_METRICS (METRIC_ID, NAME, VALUE) VALUES (UUID_STRING(), :name, :value);
$$;

