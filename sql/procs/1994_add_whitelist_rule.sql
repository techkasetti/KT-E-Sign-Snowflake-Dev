CREATE OR REPLACE PROCEDURE DOCGEN.ADD_WHITELIST_RULE(name STRING, criteria VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.WHITELIST_RULES (RULE_ID, NAME, CRITERIA) VALUES (UUID_STRING(), :name, :criteria);
$$;

