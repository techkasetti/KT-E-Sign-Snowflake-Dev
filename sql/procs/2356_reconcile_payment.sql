CREATE OR REPLACE PROCEDURE DOCGEN.RECONCILE_PAYMENT(payment_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PAYMENT_RECONCILIATION (RECON_ID, PAYMENT_ID, STATUS, DETAILS)
VALUES (UUID_STRING(), :payment_id, :status, :details);
$$

