CREATE OR REPLACE PROCEDURE DOCGEN.ASSIGN_OPERATOR_ROLE(role_name STRING, user_name STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$ INSERT INTO DOCGEN.OPERATOR_ROLES (ROLE_ID, ROLE_NAME, DESCRIPTION) VALUES (UUID_STRING(), :role_name, 'Assigned via procedure'); $$;

