CREATE OR REPLACE PROCEDURE DOCGEN.REGISTER_PROVIDER_FALLBACK_04(fallback_id STRING, primary_provider STRING, fallback_providers ARRAY)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PROVIDER_FALLBACKS_04 (FALLBACK_ID, PRIMARY_PROVIDER, FALLBACK_PROVIDERS) VALUES (:fallback_id, :primary_provider, :fallback_providers);
$$

