CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_BATCH_JOB(job_id STRING, name STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.BATCH_JOBS (JOB_ID, NAME, PAYLOAD, STATUS) VALUES (:job_id, :name, :payload, 'QUEUED');
$$

