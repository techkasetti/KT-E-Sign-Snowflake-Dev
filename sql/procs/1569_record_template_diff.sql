CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_TEMPLATE_DIFF(template_id STRING, from_v INT, to_v INT, diff TEXT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TEMPLATE_DIFFS (DIFF_ID, TEMPLATE_ID, FROM_VERSION, TO_VERSION, DIFF) VALUES (UUID_STRING(), :template_id, :from_v, :to_v, :diff);
$$;

