CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_PROVIDER_RECOMMENDATION(request_id STRING, ranked_providers VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PROVIDER_RECOMMENDATIONS (REC_ID, REQUEST_ID, RANKED_PROVIDERS) VALUES (UUID_STRING(), :request_id, :ranked_providers);
$$

