CREATE OR REPLACE PROCEDURE DOCGEN.REQUEST_VECTOR_RESTORE_12(req_id STRING, backup_id STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.VECTOR_RESTORE_REQUESTS_12 (REQ_ID, BACKUP_ID, STATUS) VALUES (:req_id, :backup_id, 'PENDING');
$$

