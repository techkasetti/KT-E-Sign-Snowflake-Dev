CREATE OR REPLACE PROCEDURE DOCGEN.ADD_OPERATIONAL_NOTE(title STRING, body CLOB)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.OPERATIONAL_NOTES (NOTE_ID, TITLE, BODY) VALUES (UUID_STRING(), :title, :body);
$$

