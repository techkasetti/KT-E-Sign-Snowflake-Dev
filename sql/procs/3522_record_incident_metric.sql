CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_INCIDENT_METRIC(incident_id STRING, metric VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INCIDENT_METRICS (METRIC_ID, INCIDENT_ID, METRIC) VALUES (UUID_STRING(), :incident_id, :metric);
$$

