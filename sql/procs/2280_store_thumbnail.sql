CREATE OR REPLACE PROCEDURE DOCGEN.STORE_THUMBNAIL(document_id STRING, size STRING, location STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.THUMBNAIL_STORE (THUMB_ID, DOCUMENT_ID, SIZE, LOCATION) VALUES (UUID_STRING(), :document_id, :size, :location);
$$;

