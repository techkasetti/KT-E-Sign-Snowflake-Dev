CREATE OR REPLACE PROCEDURE DOCGEN.REGISTER_INDEX_SHARD_MANIFEST(index_name STRING, shards VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INDEX_SHARD_MANIFEST (MANIFEST_ID, INDEX_NAME, SHARDS) VALUES (UUID_STRING(), :index_name, :shards);
$$

