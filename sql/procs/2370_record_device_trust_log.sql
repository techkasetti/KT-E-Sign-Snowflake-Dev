CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_DEVICE_TRUST_LOG(trust_id STRING, action STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DEVICE_TRUST_LOGS (LOG_ID, TRUST_ID, ACTION, DETAILS) VALUES (UUID_STRING(), :trust_id, :action, :details);
$$

