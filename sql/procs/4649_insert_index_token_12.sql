CREATE OR REPLACE PROCEDURE DOCGEN.INSERT_INDEX_TOKEN_12(token_id STRING, doc_index_id STRING, token STRING, position NUMBER)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INDEX_TOKENS_12 (TOKEN_ID, DOC_INDEX_ID, TOKEN, POSITION) VALUES (:token_id, :doc_index_id, :token, :position);
$$

