CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_RETENTION_EXCEPTION(target_ref STRING, reason STRING, approved_by STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RETENTION_EXCEPTIONS (EXC_ID, TARGET_REF, REASON, APPROVED_BY) VALUES (UUID_STRING(), :target_ref, :reason, :approved_by);
$$

