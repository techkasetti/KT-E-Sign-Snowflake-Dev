CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_EXPORT_STATUS(export_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.EXPORT_STATUS_HISTORY (STATUS_ID, EXPORT_ID, STATUS, DETAILS) VALUES (UUID_STRING(), :export_id, :status, :details);
$$

