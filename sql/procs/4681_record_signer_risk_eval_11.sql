CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_SIGNER_RISK_EVAL_11(eval_id STRING, signer_id STRING, config_id STRING, score NUMBER, detail VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SIGNER_RISK_EVALS_11 (EVAL_ID, SIGNER_ID, CONFIG_ID, SCORE, DETAIL) VALUES (:eval_id, :signer_id, :config_id, :score, :detail);
$$

