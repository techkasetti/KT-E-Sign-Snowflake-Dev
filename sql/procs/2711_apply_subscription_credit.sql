CREATE OR REPLACE PROCEDURE DOCGEN.APPLY_SUBSCRIPTION_CREDIT(account_id STRING, amount NUMBER, reason STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SUBSCRIPTION_CREDITS (CREDIT_ID, ACCOUNT_ID, AMOUNT, REASON) VALUES (UUID_STRING(), :account_id, :amount, :reason);
$$

