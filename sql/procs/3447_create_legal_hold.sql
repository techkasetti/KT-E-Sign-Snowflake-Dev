CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_LEGAL_HOLD(target_ref STRING, requestor STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.LEGAL_HOLD_REQUESTS (HOLD_ID, TARGET_REF, REQUESTOR) VALUES (UUID_STRING(), :target_ref, :requestor);
$$

