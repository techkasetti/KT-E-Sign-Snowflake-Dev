CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_INCIDENT_RESPONSE_RUN(playbook_id STRING, incident_id STRING, result VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INCIDENT_RESPONSE_RUNS (RUN_ID, PLAYBOOK_ID, INCIDENT_ID, RESULT) VALUES (UUID_STRING(), :playbook_id, :incident_id, :result);
$$

