CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_FLAG_AUDIT(flag_id STRING, changed_by STRING, new_value STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.FLAG_AUDIT (AUDIT_ID, FLAG_ID, CHANGED_BY, NEW_VALUE) VALUES (UUID_STRING(), :flag_id, :changed_by, :new_value);
$$

