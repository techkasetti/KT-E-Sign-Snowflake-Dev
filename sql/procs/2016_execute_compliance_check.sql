CREATE OR REPLACE PROCEDURE DOCGEN.EXECUTE_COMPLIANCE_CHECK(template_id STRING, context VARIANT, result VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.COMPLIANCE_EXECUTIONS (EXEC_ID, TEMPLATE_ID, CONTEXT, RESULT) VALUES (UUID_STRING(), :template_id, :context, :result);
$$;

