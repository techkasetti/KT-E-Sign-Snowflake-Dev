CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_TEMPLATE_ACCESS_REQUEST(template_id STRING, requestor STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TEMPLATE_ACCESS_REQUESTS (REQ_ID, TEMPLATE_ID, REQUESTOR) VALUES (UUID_STRING(), :template_id, :requestor);
$$
