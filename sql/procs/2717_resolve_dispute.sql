CREATE OR REPLACE PROCEDURE DOCGEN.RESOLVE_DISPUTE(dispute_id STRING, resolved_by STRING, resolution STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DISPUTE_RESOLUTIONS (RES_ID, DISPUTE_ID, RESOLVED_BY, RESOLUTION) VALUES (UUID_STRING(), :dispute_id, :resolved_by, :resolution);
$$

