CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_REQUEST_ROUTING(request_id STRING, route_ref STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.REQUEST_ROUTING_HISTORY (RH_ID, REQUEST_ID, ROUTE_REF, DETAILS) VALUES (UUID_STRING(), :request_id, :route_ref, :details);
$$

