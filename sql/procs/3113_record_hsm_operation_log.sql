CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_HSM_OPERATION_LOG(req_id STRING, status STRING, response VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.HSM_OPERATION_LOGS (LOG_ID, REQ_ID, STATUS, RESPONSE) VALUES (UUID_STRING(), :req_id, :status, :response);
$$

