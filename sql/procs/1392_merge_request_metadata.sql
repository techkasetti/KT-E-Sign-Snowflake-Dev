CREATE OR REPLACE PROCEDURE DOCGEN.MERGE_REQUEST_METADATA(request_id STRING, metadata VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$ UPDATE DOCGEN.SIGNATURE_REQUESTS SET METADATA = OBJECT_MERGE(METADATA, :metadata) WHERE REQUEST_ID = :request_id;
$$;

