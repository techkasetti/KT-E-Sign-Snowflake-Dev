CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_PIN_ATTEMPT_01(session_id STRING, success BOOLEAN, source_ip STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SIGNING_PIN_ATTEMPTS_01 (ATTEMPT_ID, SESSION_ID, SUCCESS, SOURCE_IP)
VALUES (UUID_STRING(), :session_id, :success, :source_ip);
$$

