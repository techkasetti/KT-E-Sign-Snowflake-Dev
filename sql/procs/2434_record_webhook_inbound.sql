CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_WEBHOOK_INBOUND(consumer_id STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.WEBHOOK_INBOUND (IN_ID, CONSUMER_ID, PAYLOAD) VALUES (UUID_STRING(), :consumer_id, :payload);
$$

