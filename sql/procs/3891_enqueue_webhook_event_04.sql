CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_WEBHOOK_EVENT_04(event_id STRING, sub_id STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.WEBHOOK_EVENTS_04 (EVENT_ID, SUB_ID, PAYLOAD, STATUS) VALUES (:event_id, :sub_id, :payload, 'PENDING');
$$

