CREATE OR REPLACE PROCEDURE DOCGEN.ASSIGN_FRAUD_CASE(incident_id STRING, assigned_to STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.FRAUD_CASE_ASSIGNMENTS (ASSIGN_ID, INCIDENT_ID, ASSIGNED_TO) VALUES (UUID_STRING(), :incident_id, :assigned_to);
$$

