CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_HSM_RESULT(req_id STRING, signature_blob STRING, status STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.HSM_SIGN_RESULTS (RESULT_ID, REQ_ID, SIGNATURE_BLOB, STATUS) VALUES (UUID_STRING(), :req_id, :signature_blob, :status);
$$;

