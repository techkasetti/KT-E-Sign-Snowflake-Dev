CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_SIGNATURE_EVENT(event_type STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SIGNATURE_EVENT_QUEUE (EVENT_ID, EVENT_TYPE, PAYLOAD) VALUES (UUID_STRING(), :event_type, :payload);
$$

