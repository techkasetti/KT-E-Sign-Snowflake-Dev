CREATE OR REPLACE PROCEDURE DOCGEN.REGISTER_SCHEMA_VERSION(schema_name STRING, version_number INT, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SCHEMA_VERSION_INDEX (VERSION_ID, SCHEMA_NAME, VERSION_NUMBER, META) VALUES (UUID_STRING(), :schema_name, :version_number, :meta);
$$

