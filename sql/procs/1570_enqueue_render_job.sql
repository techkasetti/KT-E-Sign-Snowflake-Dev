CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_RENDER_JOB(assembly_id STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RENDER_QUEUE (RENDER_JOB_ID, ASSEMBLY_ID) VALUES (UUID_STRING(), :assembly_id);
$$;

