CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_WEBHOOK_PROCESS_LOG(in_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.WEBHOOK_PROCESS_LOGS (LOG_ID, IN_ID, STATUS, DETAILS) VALUES (UUID_STRING(), :in_id, :status, :details);
$$

