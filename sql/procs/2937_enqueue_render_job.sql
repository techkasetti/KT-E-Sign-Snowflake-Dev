CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_RENDER_JOB(job_id STRING, document_id STRING, priority INT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RENDER_QUEUE (QUEUE_ID, JOB_ID, DOCUMENT_ID, PRIORITY) VALUES (UUID_STRING(), :job_id, :document_id, :priority);
$$

