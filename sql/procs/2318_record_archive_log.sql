CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_ARCHIVE_LOG(manifest_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ARCHIVE_LOGS (LOG_ID, MANIFEST_ID, STATUS, DETAILS) VALUES (UUID_STRING(), :manifest_id, :status, :details);
$$;

