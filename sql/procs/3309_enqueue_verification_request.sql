CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_VERIFICATION_REQUEST(bundle_id STRING, requested_by STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.VERIFICATION_REQUESTS (REQ_ID, BUNDLE_ID, REQUESTED_BY) VALUES (UUID_STRING(), :bundle_id, :requested_by);
$$

