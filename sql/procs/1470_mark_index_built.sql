CREATE OR REPLACE PROCEDURE DOCGEN.MARK_INDEX_BUILT(index_id STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
UPDATE DOCGEN.EMBED_INDEX_STATUS SET STATUS = 'READY', LAST_BUILT_AT = CURRENT_TIMESTAMP() WHERE INDEX_ID = :index_id;
$$;

