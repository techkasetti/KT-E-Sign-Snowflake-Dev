CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_DOCUMENT_SNAPSHOT_12(snap_id STRING, bundle_id STRING, snapshot_ref STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DOCUMENT_SNAPSHOTS_12 (SNAP_ID, BUNDLE_ID, SNAPSHOT_REF) VALUES (:snap_id, :bundle_id, :snapshot_ref);
$$

