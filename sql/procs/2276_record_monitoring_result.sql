CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_MONITORING_RESULT(check_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.MONITORING_RESULTS (RESULT_ID, CHECK_ID, STATUS, DETAILS) VALUES (UUID_STRING(), :check_id, :status, :details);
$$

