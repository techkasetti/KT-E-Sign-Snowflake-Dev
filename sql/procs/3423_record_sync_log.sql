CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_SYNC_LOG(job_id STRING, event STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SYNC_LOGS (LOG_ID, JOB_ID, EVENT, DETAILS) VALUES (UUID_STRING(), :job_id, :event, :details);
$$

