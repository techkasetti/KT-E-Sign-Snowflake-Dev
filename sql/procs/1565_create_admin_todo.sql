CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_ADMIN_TODO(assigned_to STRING, task STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ADMIN_TODO (TODO_ID, ASSIGNED_TO, TASK) VALUES (UUID_STRING(), :assigned_to, :task);
$$;

