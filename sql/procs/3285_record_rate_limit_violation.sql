CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_RATE_LIMIT_VIOLATION(profile_id STRING, key_ref STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RATE_LIMIT_VIOLATIONS (VIOLATION_ID, PROFILE_ID, KEY_REF, DETAILS) VALUES (UUID_STRING(), :profile_id, :key_ref, :details);
$$

