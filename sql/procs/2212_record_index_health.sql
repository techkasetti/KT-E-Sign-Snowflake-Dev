CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_INDEX_HEALTH(index_ref STRING, metrics VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INDEX_HEALTH (HEALTH_ID, INDEX_REF, METRICS) VALUES (UUID_STRING(), :index_ref, :metrics);
$$

