CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_AUDIT_SNAPSHOT_MANIFEST(scope VARIANT, snapshot_location STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.AUDIT_SNAPSHOT_MANIFEST (MANIFEST_ID, SCOPE, SNAPSHOT_LOCATION) VALUES (UUID_STRING(), :scope, :snapshot_location);
$$;

