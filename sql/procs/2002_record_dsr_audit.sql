CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_DSR_AUDIT(dsr_id STRING, action STRING, performed_by STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DSR_AUDIT (AUDIT_ID, DSR_ID, ACTION, PERFORMED_BY, META) VALUES (UUID_STRING(), :dsr_id, :action, :performed_by, :meta);
$$;

