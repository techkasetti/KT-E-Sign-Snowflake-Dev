CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_SESSION_TRANSCRIPT(session_id STRING, role STRING, message STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SIGNING_SESSION_TRANSCRIPTS (TRANSCRIPT_ID, SESSION_ID, ROLE, MESSAGE) VALUES (UUID_STRING(), :session_id, :role, :message);
$$

