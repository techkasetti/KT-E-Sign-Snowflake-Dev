CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_USER_CONSENT(signer_id STRING, tc_id STRING, version_number INT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.USER_CONSENT_LOG (CONSENT_LOG_ID, SIGNER_ID, TC_ID, VERSION_NUMBER) VALUES (UUID_STRING(), :signer_id, :tc_id, :version_number);
$$;

