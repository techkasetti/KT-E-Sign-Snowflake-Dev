CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_AUDIT_SNAPSHOT_RUN(manifest_id STRING, status STRING, result VARIANT, ended_at TIMESTAMP_LTZ)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.AUDIT_SNAPSHOT_RUNS (RUN_ID, MANIFEST_ID, STATUS, RESULT, ENDED_AT) VALUES (UUID_STRING(), :manifest_id, :status, :result, :ended_at);
$$

