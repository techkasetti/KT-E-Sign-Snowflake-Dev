CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_WEBHOOK_RETRY_04(retry_id STRING, event_id STRING, attempt INT, next_attempt_at TIMESTAMP_LTZ, last_error VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.WEBHOOK_RETRIES_04 (RETRY_ID, EVENT_ID, ATTEMPT, NEXT_ATTEMPT_AT, LAST_ERROR) VALUES (:retry_id, :event_id, :attempt, :next_attempt_at, :last_error);
$$

