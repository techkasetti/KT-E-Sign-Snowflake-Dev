CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_ALERT(alert_name STRING, level STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ALERT_LOG (ALERT_ID, ALERT_NAME, LEVEL, PAYLOAD) VALUES (UUID_STRING(), :alert_name, :level, :payload);
$$;

