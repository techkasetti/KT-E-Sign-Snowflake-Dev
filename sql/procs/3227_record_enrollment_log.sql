CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_ENROLLMENT_LOG(request_id STRING, event_type STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ENROLLMENT_LOGS (LOG_ID, REQUEST_ID, EVENT_TYPE, PAYLOAD) VALUES (UUID_STRING(), :request_id, :event_type, :payload);
$$

