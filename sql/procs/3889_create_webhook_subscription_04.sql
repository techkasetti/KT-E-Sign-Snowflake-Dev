CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_WEBHOOK_SUBSCRIPTION_04(sub_id STRING, target_url STRING, event_types ARRAY, secret_hash STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.WEBHOOK_SUBSCRIPTIONS_04 (SUB_ID, TARGET_URL, EVENT_TYPES, SECRET_HASH) VALUES (:sub_id, :target_url, :event_types, :secret_hash);
$$

