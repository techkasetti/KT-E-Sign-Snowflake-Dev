CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_VERIFICATION_RESULT(bundle_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.VERIFICATION_RESULTS (RESULT_ID, BUNDLE_ID, STATUS, DETAILS) VALUES (UUID_STRING(), :bundle_id, :status, :details);
$$

