CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_VERIFICATION_RESULT(req_id STRING, result VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.VERIFICATION_RESULTS (VERIF_ID, REQ_ID, RESULT) VALUES (UUID_STRING(), :req_id, :result);
$$

