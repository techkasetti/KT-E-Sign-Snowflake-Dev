CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_SESSION_AUDIT_05(session_id STRING, action STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SESSION_AUDIT_05 (AUDIT_ID, SESSION_ID, ACTION, PAYLOAD) VALUES (UUID_STRING(), :session_id, :action, :payload);
$$

