CREATE OR REPLACE PROCEDURE DOCGEN.COMPUTE_THREAT_SCORE_13(score_id STRING, subject_ref STRING, score NUMBER, reason VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.THREAT_SCORES_13 (SCORE_ID, SUBJECT_REF, SCORE, REASON) VALUES (:score_id, :subject_ref, :score, :reason);
$$

