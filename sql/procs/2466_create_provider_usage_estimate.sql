CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_PROVIDER_USAGE_ESTIMATE(provider_id STRING, est_window VARIANT, est_metrics VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PROVIDER_USAGE_ESTIMATES (EST_ID, PROVIDER_ID, EST_WINDOW, EST_METRICS) VALUES (UUID_STRING(), :provider_id, :est_window, :est_metrics);
$$

