CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_ANNOTATION_AUDIT(annot_id STRING, action STRING, by_user STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ANNOTATION_AUDIT (AUDIT_ID, ANNOT_ID, ACTION, BY_USER) VALUES (UUID_STRING(), :annot_id, :action, :by_user);
$$

