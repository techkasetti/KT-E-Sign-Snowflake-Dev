CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_SAML_ASSERTION(idp_id STRING, assertion VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SAML_AUDIT (AUDIT_ID, IDP_ID, ASSERTION) VALUES (UUID_STRING(), :idp_id, :assertion);
$$

