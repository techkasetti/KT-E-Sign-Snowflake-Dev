CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_MAINTENANCE_EVENT_05(event_id STRING, window_id STRING, description STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.MAINTENANCE_EVENTS_05 (EVENT_ID, WINDOW_ID, DESCRIPTION) VALUES (:event_id, :window_id, :description);
$$

