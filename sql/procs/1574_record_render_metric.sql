CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_RENDER_METRIC(render_job_id STRING, duration_ms NUMBER, status STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RENDER_METRICS (METRIC_ID, RENDER_JOB_ID, DURATION_MS, STATUS) VALUES (UUID_STRING(), :render_job_id, :duration_ms, :status);
$$;

