CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_SUPPORT_TICKET(account_id STRING, subject STRING, description STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SUPPORT_TICKETS (TICKET_ID, ACCOUNT_ID, SUBJECT, DESCRIPTION) VALUES (UUID_STRING(), :account_id, :subject, :description);
$$

