CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_RETENTION_EXCEPTION(target_ref STRING, reason STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RETENTION_EXCEPTIONS (EX_ID, TARGET_REF, REASON) VALUES (UUID_STRING(), :target_ref, :reason);
$$;

