CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_BACKFILL_REQUEST(target_table STRING, query VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.BACKFILL_REQUESTS (REQ_ID, TARGET_TABLE, QUERY) VALUES (UUID_STRING(), :target_table, :query);
$$

