CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_CONTENT_FLAG_07(flag_id STRING, document_id STRING, flag_type STRING, reported_by STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.CONTENT_FLAGS_07 (FLAG_ID, DOCUMENT_ID, FLAG_TYPE, REPORTED_BY) VALUES (:flag_id, :document_id, :flag_type, :reported_by);
$$

