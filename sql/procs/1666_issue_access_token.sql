CREATE OR REPLACE PROCEDURE DOCGEN.ISSUE_ACCESS_TOKEN(subject STRING, scope ARRAY, expires_at TIMESTAMP_LTZ)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ACCESS_TOKENS (TOKEN_ID, SUBJECT, SCOPE, EXPIRES_AT) VALUES (UUID_STRING(), :subject, :scope, :expires_at);
$$;

