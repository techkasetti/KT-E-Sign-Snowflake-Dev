CREATE OR REPLACE PROCEDURE DOCGEN.GRANT_EXPORT_PERMISSION(role_name STRING, actions ARRAY)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.EXPORT_PERMISSIONS (PERM_ID, ROLE_NAME, ACTIONS) VALUES (UUID_STRING(), :role_name, :actions);
$$

