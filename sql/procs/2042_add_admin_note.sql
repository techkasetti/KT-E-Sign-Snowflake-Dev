CREATE OR REPLACE PROCEDURE DOCGEN.ADD_ADMIN_NOTE(entity_ref STRING, note TEXT, author STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ADMIN_NOTES (NOTE_ID, ENTITY_REF, NOTE, AUTHOR) VALUES (UUID_STRING(), :entity_ref, :note, :author);
$$;

