CREATE OR REPLACE PROCEDURE DOCGEN.EMIT_INTEGRATION_EVENT(integration_id STRING, event_type STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INTEGRATION_EVENTS (IE_ID, INTEGRATION_ID, EVENT_TYPE, PAYLOAD) VALUES (UUID_STRING(), :integration_id, :event_type, :payload);
$$;

