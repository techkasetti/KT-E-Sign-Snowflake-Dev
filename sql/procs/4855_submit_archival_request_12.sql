CREATE OR REPLACE PROCEDURE DOCGEN.SUBMIT_ARCHIVAL_REQUEST_12(req_id STRING, target STRING, format STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ARCHIVAL_REQUESTS_12 (REQ_ID, TARGET, FORMAT, STATUS) VALUES (:req_id, :target, :format, 'PENDING');
$$

