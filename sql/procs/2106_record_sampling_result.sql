CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_SAMPLING_RESULT(job_id STRING, sample VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SAMPLING_RESULTS (RESULT_ID, JOB_ID, SAMPLE) VALUES (UUID_STRING(), :job_id, :sample);
$$

