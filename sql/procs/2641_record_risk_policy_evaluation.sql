CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_RISK_POLICY_EVALUATION(rule_id STRING, context VARIANT, result VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RISK_POLICY_EVALUATIONS (EVAL_ID, RULE_ID, CONTEXT, RESULT) VALUES (UUID_STRING(), :rule_id, :context, :result);
$$

