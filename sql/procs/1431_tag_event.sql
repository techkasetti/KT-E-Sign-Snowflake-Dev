CREATE OR REPLACE PROCEDURE DOCGEN.TAG_EVENT(event_id STRING, label STRING, tagged_by STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SIGNATURE_EVENT_LABELS (LABEL_ID, EVENT_ID, LABEL, TAGGED_BY) VALUES (UUID_STRING(), :event_id, :label, :tagged_by);
$$;

