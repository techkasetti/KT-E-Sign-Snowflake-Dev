CREATE OR REPLACE PROCEDURE DOCGEN.INDEX_AUDIT_ENTRY(audit_id STRING, entity_ref STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SIGNATURE_AUDIT_INDEX (IDX_ID, AUDIT_ID, ENTITY_REF) VALUES (UUID_STRING(), :audit_id, :entity_ref);
$$;

