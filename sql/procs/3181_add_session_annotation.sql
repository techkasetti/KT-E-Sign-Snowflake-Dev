CREATE OR REPLACE PROCEDURE DOCGEN.ADD_SESSION_ANNOTATION(session_id STRING, annotation STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SESSION_ANNOTATIONS (ANNO_ID, SESSION_ID, ANNOTATION) VALUES (UUID_STRING(), :session_id, :annotation);
$$

