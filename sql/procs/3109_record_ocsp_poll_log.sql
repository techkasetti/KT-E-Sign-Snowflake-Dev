CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_OCSP_POLL_LOG(schedule_id STRING, status STRING, response VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.OCSP_POLL_LOGS (LOG_ID, SCHEDULE_ID, STATUS, RESPONSE) VALUES (UUID_STRING(), :schedule_id, :status, :response);
$$

