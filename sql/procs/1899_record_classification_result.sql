CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_CLASSIFICATION_RESULT(request_id STRING, rule_id STRING, result VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.CLASSIFICATION_RESULTS (CR_ID, REQUEST_ID, RULE_ID, RESULT)
VALUES (UUID_STRING(), :request_id, :rule_id, :result);
$$;

