-- Register a FAISS snapshot manifest after a build for retrieval usage. @31 @24 @52
CREATE OR REPLACE PROCEDURE DOCGEN.REGISTER_INDEX_SNAPSHOT(snapshot_id STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$ INSERT INTO DOCGEN.SIGNATURE_INDEX_METADATA (INDEX_ID, SNAPSHOT_ID, META) VALUES (UUID_STRING(), :snapshot_id, :meta); $$;

