CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_ACCESS_AUDIT_09(audit_id STRING, resource_ref STRING, principal STRING, action STRING, detail VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ACCESS_AUDIT_09 (AUDIT_ID, RESOURCE_REF, PRINCIPAL, ACTION, DETAIL)
VALUES (:audit_id, :resource_ref, :principal, :action, :detail);
$$

