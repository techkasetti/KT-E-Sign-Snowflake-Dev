CREATE OR REPLACE PROCEDURE DOCGEN.SUBSCRIBE_ALERT(alert_type STRING, subscriber STRING, channel STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$ INSERT INTO DOCGEN.ALERT_SUBSCRIPTIONS (SUB_ID, ALERT_TYPE, SUBSCRIBER, CHANNEL) VALUES (UUID_STRING(), :alert_type, :subscriber, :channel); $$;

