CREATE OR REPLACE PROCEDURE DOCGEN.ENQUEUE_RETENTION_JOB(policy_id STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.RETENTION_JOBS (JOB_ID, POLICY_ID, META) VALUES (UUID_STRING(), :policy_id, :meta);
$$

