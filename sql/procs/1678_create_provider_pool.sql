CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_PROVIDER_POOL(name STRING, providers ARRAY)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PROVIDER_POOLS (POOL_ID, NAME, PROVIDERS) VALUES (UUID_STRING(), :name, :providers);
$$;

