CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_POLICY_EVALUATION(policy_id STRING, context VARIANT, result VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.POLICY_EVALUATIONS (EVAL_ID, POLICY_ID, CONTEXT, RESULT) VALUES (UUID_STRING(), :policy_id, :context, :result);
$$

