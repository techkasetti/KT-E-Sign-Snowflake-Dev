CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_CONVERSATION_SESSION(bundle_id STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.CONVERSATION_SESSIONS (SESSION_ID, BUNDLE_ID, META) VALUES (UUID_STRING(), :bundle_id, :meta);
$$

