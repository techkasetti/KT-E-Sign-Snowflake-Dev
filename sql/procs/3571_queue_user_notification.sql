CREATE OR REPLACE PROCEDURE DOCGEN.QUEUE_USER_NOTIFICATION(user_ref STRING, channel STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.USER_NOTIFICATIONS (NOTIF_ID, USER_REF, CHANNEL, PAYLOAD) VALUES (UUID_STRING(), :user_ref, :channel, :payload);
$$

