CREATE OR REPLACE PROCEDURE DOCGEN.SCHEDULE_REPORT_JOB(report_id STRING, cron_expr STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SCHEDULED_REPORT_JOBS (JOB_ID, REPORT_ID, CRON_EXPR) VALUES (UUID_STRING(), :report_id, :cron_expr);
$$;

