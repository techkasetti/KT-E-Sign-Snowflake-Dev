CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_REPORT_SUBSCRIPTION(report_id STRING, schedule VARIANT, destination VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.REPORT_SUBSCRIPTIONS (SUB_ID, REPORT_ID, SCHEDULE, DESTINATION) VALUES (UUID_STRING(), :report_id, :schedule, :destination);
$$

