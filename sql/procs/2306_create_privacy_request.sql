CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_PRIVACY_REQUEST(subject_id STRING, request_type STRING, payload VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PRIVACY_REQUESTS (PR_ID, SUBJECT_ID, REQUEST_TYPE, PAYLOAD) VALUES (UUID_STRING(), :subject_id, :request_type, :payload);
$$

