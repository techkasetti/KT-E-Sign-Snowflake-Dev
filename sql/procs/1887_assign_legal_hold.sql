CREATE OR REPLACE PROCEDURE DOCGEN.ASSIGN_LEGAL_HOLD(policy_id STRING, target_ref STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.HOLD_ASSIGNMENTS (ASSIGN_ID, HOLD_POLICY_ID, TARGET_REF)
VALUES (UUID_STRING(), :policy_id, :target_ref);
$$;

