CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_AUDIT_EXPORT(manifest_id STRING, target_stage STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.AUDIT_EXPORTS (EXPORT_ID, MANIFEST_ID, TARGET_STAGE) VALUES (UUID_STRING(), :manifest_id, :target_stage);
$$

