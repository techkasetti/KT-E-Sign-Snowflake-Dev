CREATE OR REPLACE PROCEDURE DOCGEN.BIND_ENDPOINT_RATE_LIMIT(endpoint STRING, profile_id STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ENDPOINT_RATE_LIMITS (LIMIT_ID, ENDPOINT, PROFILE_ID) VALUES (UUID_STRING(), :endpoint, :profile_id);
$$

