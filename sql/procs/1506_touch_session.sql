CREATE OR REPLACE PROCEDURE DOCGEN.TOUCH_SESSION(session_id STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
UPDATE DOCGEN.SIGNER_SESSIONS SET LAST_ACTIVITY_AT = CURRENT_TIMESTAMP() WHERE SESSION_ID = :session_id;
$$;

