CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_DEPLOYMENT(env STRING, version STRING, status STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DEPLOYMENTS (DEPLOY_ID, ENV, VERSION, STATUS, META) VALUES (UUID_STRING(), :env, :version, :status, :meta);
$$

