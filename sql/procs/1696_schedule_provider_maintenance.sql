CREATE OR REPLACE PROCEDURE DOCGEN.SCHEDULE_PROVIDER_MAINTENANCE(provider_id STRING, scheduled_at TIMESTAMP_LTZ, duration_min INT, notes VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.PROVIDER_MAINTENANCE (MAINT_ID, PROVIDER_ID, SCHEDULED_AT, DURATION_MIN, NOTES) VALUES (UUID_STRING(), :provider_id, :scheduled_at, :duration_min, :notes);
$$; 
-- @31 @24 @52

