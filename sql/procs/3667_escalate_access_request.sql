CREATE OR REPLACE PROCEDURE DOCGEN.ESCALATE_ACCESS_REQUEST(request_id STRING, escalated_to STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ACCESS_REQUEST_ESCALATIONS (ESC_ID, REQUEST_ID, ESCALATED_TO) VALUES (UUID_STRING(), :request_id, :escalated_to);
$$

