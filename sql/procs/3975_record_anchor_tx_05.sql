CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_ANCHOR_TX_05(tx_id STRING, bundle_id STRING, anchor_type STRING, anchor_ref STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ANCHOR_TRANSACTIONS_05 (TX_ID, BUNDLE_ID, ANCHOR_TYPE, ANCHOR_REF) VALUES (:tx_id, :bundle_id, :anchor_type, :anchor_ref);
$$

