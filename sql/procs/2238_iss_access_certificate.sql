CREATE OR REPLACE PROCEDURE DOCGEN.ISSUE_ACCESS_CERTIFICATE(subject STRING, scope VARIANT, expires_at TIMESTAMP_LTZ)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.ACCESS_CERTIFICATES (CERT_ID, SUBJECT, SCOPE, EXPIRES_AT) VALUES (UUID_STRING(), :subject, :scope, :expires_at);
$$

