CREATE OR REPLACE PROCEDURE DOCGEN.STORE_SEARCH_SUMMARY(query STRING, results VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.SEARCH_SUMMARIES (SUMMARY_ID, QUERY, RESULTS) VALUES (UUID_STRING(), :query, :results);
$$

