CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_ZIP_MANIFEST(bundle_id STRING, files ARRAY, zip_location STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.DOCUMENT_ZIP_MANIFESTS (MANIFEST_ID, BUNDLE_ID, FILES, ZIP_LOCATION) VALUES (UUID_STRING(), :bundle_id, :files, :zip_location);
$$;

