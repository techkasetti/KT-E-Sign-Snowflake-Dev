CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_KPI_DEFINITION(name STRING, definition VARIANT, owner STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.KPI_DEFINITIONS (KPI_ID, NAME, DEFINITION, OWNER) VALUES (UUID_STRING(), :name, :definition, :owner);
$$

