CREATE OR REPLACE PROCEDURE DOCGEN.RECORD_TENANT_SYNC(mapping_id STRING, status STRING, details VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.TENANT_SYNC_HISTORY (HIST_ID, MAPPING_ID, STATUS, DETAILS) VALUES (UUID_STRING(), :mapping_id, :status, :details);
$$

