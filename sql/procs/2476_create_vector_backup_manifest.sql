CREATE OR REPLACE PROCEDURE DOCGEN.CREATE_VECTOR_BACKUP_MANIFEST(snapshot_path STRING, meta VARIANT)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.VECTOR_BACKUP_MANIFEST (MANIFEST_ID, SNAPSHOT_PATH, META) VALUES (UUID_STRING(), :snapshot_path, :meta);
$$

