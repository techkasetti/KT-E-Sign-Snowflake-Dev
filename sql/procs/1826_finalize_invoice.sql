CREATE OR REPLACE PROCEDURE DOCGEN.FINALIZE_INVOICE(draft_id STRING, external_ref STRING)
RETURNS VARIANT
LANGUAGE SQL
AS
$$
INSERT INTO DOCGEN.INVOICE_FINALIZED (INVOICE_ID, DRAFT_ID, EXTERNAL_REF) VALUES (UUID_STRING(), :draft_id, :external_ref);
$$;

