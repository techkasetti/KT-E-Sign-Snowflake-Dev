-- Task to run backfill worker that processes EVENTS_BACKFILL_QUEUE items @1 @6.
CREATE OR REPLACE TASK DOCGEN.TASK_RUN_BACKFILL WAREHOUSE = WH_PROC SCHEDULE = 'USING CRON 0 3 * * * UTC' AS CALL DOCGEN.PROCESS_EVENTS_BACKFILL();

